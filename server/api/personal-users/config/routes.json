{
  "routes": [
    {
      "method": "GET",
      "path": "/personal-users",
      "handler": "personal-users.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/personal-users/count",
      "handler": "personal-users.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/personal-users/:id",
      "handler": "personal-users.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/personal-users",
      "handler": "personal-users.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/personal-users/:id",
      "handler": "personal-users.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/personal-users/:id",
      "handler": "personal-users.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
